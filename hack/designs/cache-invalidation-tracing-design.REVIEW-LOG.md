# Cache-invalidation tracing design — adversarial review log

Reviewer: Codex XHIGH `tc-e33b0cb30796e442e5ed18271dcb30db` (dagger1), fresh fork per round,
same reviewer across rounds (context accumulates). Design commits on branch
`cache-inv-tracing-design` (off whatif feature tip `4bc3d9404`).

| Round | Design commit reviewed | Verdict | Load-bearing findings → disposition |
|---|---|---|---|
| 1 | `d2478f4e6` (r1, initial) | REJECT | Terminal enumeration incomplete (digest-only lookup entry; persisted-load-failure; join-key wording; reset paths) → ACCEPTED, §2 completeness notes + category 9. Categories 5/6 (TTL-expired / session-filtered) underivable offline → CONFIRMED, E1 justified. Cross-source input parity FALSE (OTel dag.inputs deduped, module-less; loader discards dag.call) → ACCEPTED as critical; E3 introduced; pair mode native-first. Category "session-lifetime" is inference → ACCEPTED, downgraded to mechanism-not-recorded. Category 1 not analyzer-available (loader discards dag.call) → ACCEPTED. Duplicate-digest walk ambiguity → RESOLVED via per-digest outcome summaries (not refusal). E1 under-specified → ACCEPTED, full capture-schema spec. |
| 2 | `3a9c8c4e2` (r2) | REJECT | Concurrency default is per-SESSION not per-client (objects.go:604-608; independently verified) → ACCEPTED. §3.1 residual parity claim → ACCEPTED, removed. E1 "miss calls only" misses hit-unusable arm (persisted_load_failed fires on a SELECTED hit) → ACCEPTED, lookup-outcome reframe. E3 tuple (field,type,nth,view) insufficient (self digest also covers args/literals/implicits) → REFUTED-ACCEPTED, E3a (OTel ordered-input parity attr) + E3b (dag.call canonical self structure via loader) split; native arg-level degrade stated. Digest-node status too strong (reversals both directions; all-hit_pending undefined) → ACCEPTED, time-aware first-demand status. digest_only enum/metadata contradiction seeds → flagged. Native do_not_cache lacks ident (returns before SetIdent) → ACCEPTED as critical, micro-emit. E3 lacked implementation chunk → ACCEPTED. |
| 3 | `6ad9e9ea5` (r3) | REJECT | OTel pre-E3 walk NOT edge-set complete (module refs absent → wrong frontier possible) → ACCEPTED as critical; walks declared PARTIAL with caveat→per-node refusal path. Greedy class-prefix pairing mispairs duplicate classes (counterexample A=[C:d1,C:d2,D:d3] vs B=[C:d2,D:d3]) → ACCEPTED as critical; digest-anchored LCS contract; counterexample pinned W16b. E1 enum contradiction (digest_only_miss vs entry-as-metadata) → ACCEPTED, removed from enum. do-not-cache ident derive must be best-effort/no behavior change → ACCEPTED. OTel seen-key suppression caveat for digest-node status; StartNS tie-break → ACCEPTED. "Precisely classifiable post-E1" too strong for release/collection reversals → ACCEPTED, retracted. |
| 4 | `21096c3c8` (r4, amended once) | RATIFY-WITH-CHANGES | Five exact edits: occurrence-unique digest-LCS refusal (+byte-identical-interval exception); §6 Cloud-readiness wording; §4 per-source CacheInputs wording; E1 scope sentence; Chunk-1/Chunk-4 dag.call parsing ownership split → ALL APPLIED. |
| 5 | `6e236c205` (r5, amended once — §6 sentence had dodged edits via line-wrap mismatch) | **RATIFIED (converged)** | Per-edit verification of all five; "no residual load-bearing issue." |
| post | `64b4b300a` (r6) | n/a (factual strengthening only, no semantic change) | §7 take-3 serving-outcome vocabulary upgraded from attributed to VERIFIED against dagql/cache_stats.go (take-3 worktree `remote-cache-take3-fork-0bdd5926` @ `2924af65ce`), with two verified convergence points. |
