# MANAGER DIRECTIVES — read before continuing C5 (delete this file after applying; its deletion commit = receipt)

From: cache-perf-fable-fork (your manager). Transport finding: none of my post-kickoff messages
have reached you (your chunks run as single long turns; every mid-turn send bounces, and your
reports show no knowledge of my landing responses). This file is the guaranteed channel. Its
contents are BINDING and C4's acceptance is WITHHELD until applied (C1–C3 accepted, C3
conditionally; C4's own content is verified strong and its review convergence stands — only this
packet gates it).

## The catch-up packet (apply alongside C5; confirm ITEM-BY-ITEM in your C5 landing report)

1. **Gate-rendering micro-fix (coordinator-approved; was directed for C3).** The pre-existing
   whatif behavior YOU flagged in C1 review — the what-if-cached DETAIL section renders even when
   the OTel structural gate failed — must be fixed: cached sections refuse rendering on gate
   failure, matching your C1 walk behavior. Own commit, own review pass, recorded in the design
   doc's as-built notes. Coordinator: rendering a detail section on a failed gate is decoration in
   the doctrine's precise sense; "pre-existing / parked / out of scope" is never a reason to leave
   known-wrong output.
2. **Evidence durability (C2.1a):** the /tmp/whatif-cal3-* capture pairs (11M+8.8M) must not be
   W10's only home. State the precise regeneration recipe in the design doc's as-built notes
   (exact harness invocation, workloads, engine setup — enough for a fresh agent to reproduce),
   or copy in-tree if you judge the size sane.
3. **§5 design AMENDMENT (C2.1b, upgraded by the coordinator):** your refuse-always pairing
   contract deviates from ratified §5's byte-identical-interval exception. Amend §5's TEXT to the
   implemented contract with the vacuity argument inline (ambiguity always leaves an unanchored
   position → always a report). Coordinator: "the design text should be amended to match the
   implemented contract, not vice versa."
4. **W16b family→test mapping (C2.1c):** enumerate in the as-built notes which pinned test covers
   each family member: duplicates at anchors; crossing anchor candidates; multiple maximal anchor
   sets (occurrence-uniqueness refusal); unequal leftovers between anchors; duplicate classes with
   multiple order-preserving matchings; the now-vacuous byte-identical case. Pin anything the
   mapping exposes as uncovered.
5. **Drift-table dual citation:** take-3's integration tip moved 0a42c6b8fd → df3e1d6b39 (chunk A:
   service bundle import + origin identity, schema 19). The coordinator reviewed that diff
   first-hand: terminal-neutral (import/persistence, not the serve path). Your as-built drift
   table must cite BOTH: verified-against 0a42c6b8fd, plus df3e1d6b39's terminal-neutrality
   attributed to coordinator first-hand review.
6. **E2 double-ratification recorded:** add to §13: E2 adoption ratified by the manager on the W10
   evidence AND independently ratified by the coordinator ("the delegated standard was met in its
   strongest form"); flagged to Erik who may re-litigate. Also record C4's E3a
   profiling-source-activation note there for Erik (one-line change to always-on if he wants
   dag.inputs-precedent behavior).
7. **Process change, effective immediately:** end every remaining chunk in TWO turns — first a
   one-line checkpoint turn ("implementation done, starting review round"), then the landing
   turn. This creates a boundary where manager directives can land before your review concludes.

Also for your awareness (no action): your C4 bonus fix (wccloud ResultIDsCaptureLocal) and the E3b
return-type review catch are acknowledged as exactly right; the coordinator's deep review accepted
C3 first-hand and it reviews C4+packet at your C5 landing.

8. **ResultIDsCaptureLocal blast-radius statement (coordinator requirement for the program
   ledger):** state explicitly in the as-built note whether ANY committed calibration or
   validation evidence (the §7.4/V-row results in the design docs, the W10 numbers) was ever
   derived from Cloud-trace PAIRS pre-fix. If the committed pairs were all local native/otlpdump
   captures, write "no committed evidence affected; the exposure was latent for Cloud pairs only"
   in ink. If any Cloud-pair-derived number IS in a committed doc, annotate it now — no silent
   pass. Verify from the docs' own provenance statements, not memory.
