Independent coordinator review, 13 September 2026

Candidate: 1ba150a79b8a3f7568e27a22cee492f578ffa538, tree d5a73331e65629681246351a864fd2ecda9b899e. Main reviewed delta: a161ceb34c74012e83d8323a8c56902a937b7c7b..1ba150a79b8a3f7568e27a22cee492f578ffa538; documentation cleanup 8a8a4dfda213d1d31067373e66a5bb0c48a9b7f2 and all six subsequent implementation commits. This review was written before reading either council member's new findings.

The cleanup preserves the intended ordinary-cache direction, but single-record live capture has two should-fix gaps. These concern the retained primitive's own contract, not the missing graph exporter or remote acquisition feature. The Human's permission to keep the commits stands.

1. **Capture can encode a row whose initial dependency attachment is still open.** In dagql/cache_persistence_capture.go:29, registration is checked but attachmentState is not. The existing persistence worker excludes unclean attachment at dagql/cache_persistence_worker.go:291. The focused probe parks the existing initial attachment hook, obtains the registered row, verifies resultAttachmentOpen, and calls capture: it succeeds with a record. Encoding at that point can inspect payload references while attachment is still replacing/establishing them. Reject non-clean attachment through the existing readiness/error convention, with a focused control proving the codec is not entered. Do not add graph import/export machinery.

2. **Capture's cache-side lazy lock does not exclude direct object-side evaluation.** core/container_parts.go:538 runs the same group bodies through evaluatePartsDirect without a cache lazy attempt. Production callers include metaFileContents in core/container_exec.go:2462 and container export in core/container.go:6877. Capture holds shared.lazyMu, but those direct runners synchronize through core.LazyState instead. core/container_persistence.go:46 then reads the object metadata before its completion checks. The deterministic probe parks a test metadata body inside the real LazyState.EvaluateGroup and evaluatePartsDirect path, after one field write and before completion. Capture succeeds and serializes WorkingDir=/half-written while consumed=false. The test uses a controlled metadata body, not an actual exec or remote transport. It establishes that the exclusion boundary misses this supported path; unsynchronized production metadata writes are the corresponding inferred race risk. Synchronize live capture with object-side mutation too, retaining per-group parallelism, no evaluation on capture, and bounded readiness behavior. Check analogous Directory/File direct evaluation boundaries as part of this fix. Do not redesign lazy acquisition.

The probes are [attachment probe](cleanup-review-evidence/attachment_probe_test.go.txt) and [direct capture probe](cleanup-review-evidence/direct_capture_probe_test.go.txt), injected only as extra test files using [probe-overlay.json](cleanup-review-evidence/probe-overlay.json). They are stored as text to avoid adding test packages to the repository. Production blobs are unchanged. [probes-v2.log](cleanup-review-evidence/probes-v2.log) records both expected-readiness assertions failing because capture returned nil error. Command: go test -p 1 -overlay cleanup-review-evidence/probe-overlay.json ./dagql ./core -run '^TestReviewCaptureDuring' -count=1 -timeout=60s -v. The earlier [probes.log](cleanup-review-evidence/probes.log) contains an attachment-fixture mistake (waiting for session ownership too early); it is not evidence of the attachment defect. Its direct-evaluation result agrees with v2.

Coverage: read the complete three investigator reports, the current repository contribution instructions and full engine-debugging skill, direct Human scope/process instructions, original removal map, all production changes in the main range, five reduced documents, added/changed tests, and surrounding attachment, persistence, lazy state, producer encoding/decoding and digest teaching code. Checked the removed-scope vocabulary and files against the retained ancestry. Ordinary egraph participation remains intact. Existing labels implement the requested safe-extra annotation without schema expansion. Changeset wrappers retain ordered filtered inputs and conflict modes, with an ordinary Directory-producing call replacing the synthetic snapshot identity. Completed producer payloads retain declared references without eagerly loading producer ancestors. I found no additional independently substantiated blocking/should-fix defect in those slices.

Validation: the full engine-debugging skill was read before choosing or interpreting tests. Selected existing tests PASS in all four packages on unmodified production candidate 1ba: dagql/call, dagql, core, core/schema. The result is recorded in [retained-focused.log](cleanup-review-evidence/retained-focused.log). Exact command: go test -p 1 ./dagql/call ./dagql ./core ./core/schema -run '^(TestCapturePersistedRecord|TestRemoteCacheExtraDigest|TestExtraDigestLabelIsolation|TestEquivalencySetCacheHits|TestCacheTeachContentDigest|TestContainerExecPersistsInputMetadata|TestContainerCompletedProducer|TestFilesystemCompletedProducerPersistence|TestContainerPersisted|TestContainerRestore|TestPersistedCoreLazyPayloadRelocation|TestCoreSchemaObjectsHavePersistedFamilies)' -count=1 -timeout=90s. This run did not use the probe overlay. No native engine suite or model run was commissioned by this review. The investigator's native Changeset/restart/generator results retain their original source-at-run limits. No claim is made for complete graph transfer, real remote-chain fallback, or two-engine behavior.

Scope limits: the four-file uncommitted graph draft remains excluded and untouched. Preserved narrow graph reviews are historical evidence. Five reduced design documents are source material for later separately authored designs and holistic review; this cleanup review does not approve the unfinished transfer/acquisition design set. A missing future feature is not itself an instruction to implement it during cleanup.
