Batch 6 rework plan — preparation only.

The worktree remains on readiness commit `4b0e088d3c87cc41205f8976c3f4dd2d08987aaa`, with the batch 4 production baseline documented there. No implementation start or reset has occurred. This plan consumes these converged sources through Git:

- `P:n`: line n of `git show 2cbca74cb354e8f70eeb265e16b4ab92c74e6c07:hack/designs/remote-cache/focused/08-lazy-values.md`, blob `96f19f1e79ab04c9cddd6489171cafc6a22d04b4`. Required §§2, 5, 6 and 6.1 were read in full.
- `E:n`: line n of `git show db36af0e45b4b9555b5de0ceee721ca39e993f47:hack/designs/remote-cache/proposed-design-explained.md`, blob `cbf5ff08c013061070c60d3b2f779b4d3be452e2`, read in full.

The proposal resolves the conditional concerns in the [readiness dependency inventory](continuation-evidence/implementation/b6/READINESS.md#L117): encoded routing and fresh private evaluation survive under Lazy names; `original`, `OriginalPermit` and `beforeSyncCleanup` remain necessary. An acquired value may retain only encoded operation data and discard its temporary restore pointer. Snapshot/acquired forms do not start decoding operation inputs merely because the live completed pointer disappears. These corrections govern future work without amending the historical readiness commit.

| Readiness inventory coverage | Renamed interface and batch 6 plan |
| --- | --- |
| [Foreground fallback](continuation-evidence/implementation/b6/READINESS.md#L123) and [pure probes](continuation-evidence/implementation/b6/READINESS.md#L124) | Consume `LazyOperationRoute`, `HasLazyOperation` and `LazyGroupAddress` in place of `PartProducerRoute`, `HasProducer` and `ProducerAddress`. `PersistedPartRouter.RouteParts`, `DescribeParts`, `NeedsMetadata` and `PartDelegation` survive. Keep full-address probing data-only: no metadata evaluation, no operation inferred without encoded inputs, no receiver exclusion because it has an operation. Ordinary fallback becomes `runLazyOperationDecision`/`joinLazyEvaluation`, using `lazyEvaluationTaskKey`, `lazyGroupAddressKey` and `errLazyEvaluationBusy`; source order and exact-parent delegation remain unchanged. P:180–186, 196, 199, 221; E:388–428. |
| [Writer exclusion](continuation-evidence/implementation/b6/READINESS.md#L125) and [private execution boundary](continuation-evidence/implementation/b6/READINESS.md#L126) | Use `LazyEvaluationPhase`, `LazyEvaluationOpen/Preparing/Running/Evaluated` and `partLazyEvaluationState`. The factory/invocation become `PersistedLazyOperationFactory`, `PrepareLazyOperation`, `LazyOperationInvocation` and `privateLazyOperation`. Place the proposed no-evaluation guard before preparation and `Run`, and at `LazyState.Evaluate`/`EvaluateGroup`; retain the other demand/service/schema/client/provider guards. Sharing still uses the common gate and NoJoin obtain task, permits synthetic installation Bodies, and never falls back to evaluation. Fresh private evaluation remains foreground work. P:182, 197–198, 222. |
| [Retained operation](continuation-evidence/implementation/b6/READINESS.md#L128) and [output stamps](continuation-evidence/implementation/b6/READINESS.md#L131) | Use `LazyState.IsEvaluated()` for whole-operation state and `GroupConsumed` plus part mappings for grouped Containers. Pointer presence is not pending work. Preserve nonblocking body/publication guards and `persistenceBody`; completion advances File/Directory `OutputRev` once per successful state transition. Restore-wrapper evaluation means opening stored output, and Container restore groups need their own finality checks. Sharing still proves the donated address's descriptor, applied role and completed bookkeeping; an unopened stored donor can qualify. P:24–34, 78, 223; E:281–287. |
| [Typed stores](continuation-evidence/implementation/b6/READINESS.md#L129) and [complete acquired views](continuation-evidence/implementation/b6/READINESS.md#L130) | `completedRecipeKind/JSON` become retained `lazyKind/lazyJSON`; the live `completedRecipe` disappears. S1's `readyPartPreparationBase` and prepared provenance must copy the receiver's complete representation, retained operation bytes, roles and pending siblings. `PreparePartRecord`/`Publish` never substitute donor operation data or a private latch. Temporary restore pointers may be dropped after supplying refs; Container's immutable acquired view remains authoritative. Keep exact predecessor checks and per-address donor validation. P:60–78, 223; E:671, 681–689. |
| [Shared publication](continuation-evidence/implementation/b6/READINESS.md#L127) | `publishProducedParts`/`prepareProducedParts` become `publishEvaluatedParts`/`prepareEvaluatedParts`. Extend the same `PreparedReadyPart` transaction while preserving `original`, its `OriginalPermit`, and `beforeSyncCleanup`. Public Prepare/Commit/Finish contracts and selective publication survive. The sharing worker still prepares all slots, commits, releases member/source holds, then externally consumes every receipt; installed outputs survive cleanup errors. P:184, 199, 224; E:302–316. |
| [Input ownership](continuation-evidence/implementation/b6/READINESS.md#L132) and [decode preflight](continuation-evidence/implementation/b6/READINESS.md#L133) | `attachCompletedProducerInput` becomes `attachLazyInput`. Attach live operation inputs even after evaluation, retaining direct ownership and `Owned: false`; Own still excludes offers at every depth. S3(a)'s defaults factory, registered preparation context and exact-reference audit remain required. Snapshot/acquired retained bytes remain data; pending native forms still decode actual typed-operation inputs. Audit those forms before entering a shared attempt, preserving receiver input edges when adding output/service/handle edges. P:62–77, 190, 225, 228; E:495–517, 537. |
| [Completion notifications](continuation-evidence/implementation/b6/READINESS.md#L134) | Converted constructors initially publish pending values. Positive donor notification follows successful lazy ownership/bookkeeping after waiter wake; then reenter E and validate registration. Keep all graph/import/publication triggers and the collector's single-E-interval contract. Eagerly evaluated cleaning retains its operation before publication. Body evaluation alone never establishes sharing readiness. P:34, 226; E:289–300, 436. |
| [Constructors](continuation-evidence/implementation/b6/READINESS.md#L135) and [resolved HTTP/Git identity](continuation-evidence/implementation/b6/READINESS.md#L136) | Consume the replacement ordinary constructors in fixtures: scratch, both mounts, builtin and schema File become lazy; HTTP uses `__httpFile`, moving Git names use `__resolvedRef`, and cleaning evaluates its constructed lazy value. The foundation owns these resolver/identity changes. Sharing relies on ordinary equivalence, never calls their resolution/evaluation paths, and keeps immutable HTTP/Git snapshots eligible. `HTTPBodyDigestMismatchError` preserves mismatch classification. P:190, 201, 226; E:543–575, 601–639. |
| [Persistence and onward export](continuation-evidence/implementation/b6/READINESS.md#L137) | Consume `OperationState`, `transferOperationState`, `validateTransferOperation` and JSON `operationState` with `none/pending/evaluated`. This is saved operation state, not proof of local bytes or a private latch to reuse. Replace the readiness note's format assumptions with schema 21/envelope 5/bundle 2, or the jointly advanced versions actually converged in the foundation. Sharing adds no further format cut; preserve installed ownership, retained bytes, input edges, pending siblings and owner-before-pin boot order. P:200, 204, 226; E:665–696. |
| [Verification](continuation-evidence/implementation/b6/READINESS.md#L138) | Use `lazy-enter`, `installed-lazy`, `lazy-ref-released`, `lazy-ref-release-error`, `TransferFixtureLazyReleaseObserver` and `partFixtureLazyContext` at their real boundaries. Exercise pending, evaluated and restored donors; raw-byte Service/Module preparation; ordered multi-part installs; foreground decode joiners; release/GC/restart; bookkeeping-only retry and direct guards. Zero evaluation must be observed at surviving entries, not inferred from deletion of an old counter. Keep the existing report groups. P:202, 213, 228; E:783–792. |

On the binding start, reset onto the named converged batch 5 head and preserve both evidence commits in order. First remap actual renamed files/symbols and recheck the readiness mismatches, including the B4-D10 outside-E lookup contract, S1 provenance, receiver admission and donated-address readiness. Then implement against the converged foundation and batch 5 lifecycle surfaces. The partial seven-commit series is not an implementation base for this plan.

The obligations change representation, names, constructor timing and verification observations. S1 ordered preparation, S2 notification after waiter wake, S3(a) service/default reconstruction, structural admission, independent ownership, finite pass/drain, real-error propagation and batch 5 offer/renewal isolation retain their meaning. Verification for this evidence is source/reference review only; no build, test or implementation change was performed in this preparation turn.
