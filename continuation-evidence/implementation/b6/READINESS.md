Batch 6 preparation evidence, before implementation authorization.

The inspected worktree is `remote-cache-b6-sharing-implementer-3ffeb586` at `22d9c17296acfc0bd6e85cce3d01c9e776501453`. Its parent is the converged batch 4 head `018a0e695b96d0849118a510d359795812571e1b`, with implementation tip `f04a91577bd4ddc574f3bc527cdffe9fec2179a1`. The only difference from that parent is batch 5's readiness document. All production-code locations below therefore describe the converged batch 4 implementation, before batch 5's implementation arrives. This document is the only repository change made for this preparation deliverable.

Implementation has not started. On the binding start message, reset this branch onto the named converged batch 5 head, then cherry-pick this signed evidence commit before the first implementation commit. Recheck the batch 5 surfaces listed below on that head. No reviewed commit is to be amended.

Subsequent coordinator steering says the Human-directed producer foundation change must be proposed, reviewed and implemented before batches 5 and 6 proceed. Every re-creatable value will remain a lazy value whether evaluated or not; the completed-producer fields and separate saved-producer route are to be replaced. The binding implementation head will therefore change. The mappings below describe the inspected baseline and D6's current assumptions; they do not require preserving the outgoing mechanism. The producer dependency section identifies what the proposal must account for. No foundation change is implemented here.

The following inputs were read through Git. `D6:n`, `D4:n` and `D5:n` below mean exact line n of the named pinned document, with ordinary one-based line numbering. The design sources are not substituted with checked-out copies; the batch 6 design path is absent from this worktree. Code links are repository-relative and refer to the inspected worktree.

| Input | Pinned source and reading |
| --- | --- |
| Commission | `fef0de6a78f527602c55693b24bfd41227dedf82:continuation-evidence/implementation-commissions/BATCH6-SHARING-IMPL.md`, in full. The coordinator branch initially resolved to `c77b908c7dcfe08f8a12b26479e73035721f2ce3`; all five requested coordinator artifacts are unchanged between those two tips. The commission was read again at the later tip. |
| D6 | `6e8f1a8d63c03aec200bd1027dd13590a2fbdf86:hack/designs/remote-cache/focused/06-snapshot-sharing.md`, blob `a70fb1c42c087225d1fd1198c9e642431e2a6b3e`, in full. |
| Sharing decisions | Coordinator sources `continuation-evidence/focused-design-reviews/06-sharing/v4-round1/CONSOLIDATED.md` and `v4-round2/CONSOLIDATED.md`, in full, at the above coordinator revisions. Round 1 lines 21–23 establish S1, S2 and S3; round 2 confirms S3(a), the explicit audited-family registry and preflight over actual decoding references. |
| D4 | `7af104b49aad9dd4c9a02b42b9d2152e59a9c8fe:hack/designs/remote-cache/focused/04-acquisition.md`, blob `d63729442180a2d7faf872b687bf8d64cf19083b`. Required §3.2 and §6 were read in full, together with the surrounding acquisition, task and decode contracts. |
| Batch 4 implementation decisions | Coordinator sources `continuation-evidence/implementation-reviews/b4/round1/CONSOLIDATED.md` and `round2/CONSOLIDATED.md`, in full. Round 1 line 28 is B4-D10; line 29 is B4-D9. B4-D2 and B4-D3 are at lines 20–21. Round 2 confirms the implementation head above. |
| D5 | `716a6311daee93e2f6d9673544f284e855968eb0:hack/designs/remote-cache/focused/05-offers-renewal.md`, blob `6ac1c003ac5ed3e5a081e98487c6a5380cc36fda`, in full. |
| Implementation and verification guidance | The four requested `dagql/cache_part_{source,install,task,demand}.go` files were read in full, together with the notification, persisted-decode and supporting ownership/publication entry points cited below. [engine-debugging skill](skills/engine-debugging/SKILL.md#L1) was read in full before verification. |

The existing acquisition interfaces and their precise differences from D6 are as follows. An absent S1/S3 interface is planned batch 6 work, not a claim that batch 4 was commissioned to implement it.

| Interface or invariant | Exact implementation | Match or difference from the design |
| --- | --- | --- |
| Sessionless constructor, outside E | `Cache.newSessionlessPartSourceLease`, [cache_part_source.go:564](dagql/cache_part_source.go#L564); lookup preparation at line 568, donor capture validation at 575, E acquisition at 578. `partLookupFor` is at [line 115](dagql/cache_part_source.go#L115). | **B4-D10 signature/lock correction:** D6:91–95 and D4:127–130 show only the locked constructor; D6:174 says create it under E. The actual safe wrapper must be entered without E. It resolves numeric call references before acquiring E. A caller choosing the locked entry must prepare `partLookup` and validate its captured probe outside E first. Calling the wrapper while holding E would violate its contract. |
| Sessionless constructor, inside E | `Cache.newSessionlessPartSourceLeaseLocked(ctx, receiver, donor, target, address, probe, lookup partLookup)`, [cache_part_source.go:585](dagql/cache_part_source.go#L585); `sessionlessPartEquivalentLocked` at [line 622](dagql/cache_part_source.go#L622). | D6:92–94 omits the implemented final `lookup` argument. The implementation revalidates receiver frame identity at line 623, registered rows/import origin at 586, probe identity/full source address at 596, ordinary route plus structural admission at 603, and takes its own donor hold at 618. This is the reviewed B4-D10 contract to consume. |
| Private sharing authority | `PartSourceLease.sessionlessShare`, [cache_part_source.go:63](dagql/cache_part_source.go#L63), set by the locked constructor at [line 607](dagql/cache_part_source.go#L607); Commit branch at [cache_part_install.go:369](dagql/cache_part_install.go#L369). | Matches D6:97: foreground session admission is a separate branch. There is no need to fabricate a session or call foreground selection for a share. B4-D9 removed the unused private descriptor/resource/ownership revision copies shown in D4:89; the actual lease keeps `version`, `facts` and `offerRev` at lines 62–68. Public probe fields remain. |
| Own resources and exact leaves | `recomputeRequiredSessionResourcesLocked`, [cache.go:611](dagql/cache.go#L611); `ownPartRequirementsFitLocked`, [cache_part_source.go:552](dagql/cache_part_source.go#L552); `partResourceLeavesLocked`, [line 246](dagql/cache_part_source.go#L246); `partSourceReferenceAllowedLocked`, [line 634](dagql/cache_part_source.go#L634). | Direct `deps` and own handles provide the stored requirement sets; offer edges are excluded. The constructor adds exact donor resource leaves and validates each copied dependency at lines 609–615. **Additional D6:103 proof is absent:** the lease records donor facts, but no immutable original receiver Own set/generation. Commit repeats current subset tests; it does not compare the receiver's current Own with its original admission set. Ordered sharing must preserve D6's original-set constraint when earlier direct edges or concurrent mutations occur. |
| Ready probe shape and mapping | `PartDescriptor`/`PartProbe`, [cache_part_source.go:25](dagql/cache_part_source.go#L25); `probePart`, [line 275](dagql/cache_part_source.go#L275); `foreignFamilyCodec.DescribeParts`, [core/part_probe.go:8](core/part_probe.go#L8). | The public fields in D6:51–69 exist, with an additional private `captured` carrier. The pure describer derives concrete full addresses through `MapSnapshotParts`. `DescriptorRev` is currently the whole payload revision at line 296; the public `OutputRev` and `OfferRev` fields are not populated by this probe. These fields alone are not the donated-address publication proof required by D6:82. |
| Sharing readiness | `capturePartRecord`, [cache_persistence_capture.go:90](dagql/cache_persistence_capture.go#L90); `probePart` busy checks, [cache_part_source.go:309](dagql/cache_part_source.go#L309); row sync, [cache.go:1653](dagql/cache.go#L1653). | **D6:78–84 adds checks not supplied by this probe:** capture substitutes desired intent at lines 94–95; it does not establish a matching applied donor role. Probe checks selected Installed state, Running overlapping groups and active writers, but does not inspect attachment completion/error, pending native bookkeeping, or whether `leaseSyncMu` is active. The constructor also does not check those facts. Sharing must establish the complete unlocked readiness proof and validate publication facts at admission/Commit. A desired role or `LocalComplete` alone is insufficient. |
| Receiver expiry | Candidate expiry filter, [cache_part_source.go:179](dagql/cache_part_source.go#L179); constructor registration check, [line 586](dagql/cache_part_source.go#L586); Commit row check, [cache_part_install.go:359](dagql/cache_part_install.go#L359). | **D6:84 and 262 require receiver expiry at selection and Commit.** The candidate collector excludes expired donors only when `row != receiver` at line 183. Neither the constructor nor Commit rejects an expired receiver. Donor expiry is rechecked through candidate collection; receiver expiry needs the explicit sharing check. |
| Separate storage protection | `SnapshotManager.PinSnapshot`, [engine/snapshots/manager.go:69](engine/snapshots/manager.go#L69), implemented at [lease.go:305](engine/snapshots/lease.go#L305); Prepare pin/accessor split at [cache_part_install.go:179](dagql/cache_part_install.go#L179). | Matches D6:121 and D4:299–301: independent pin, ancestry attachment before opening, `NoUpdateLastUsed`, and a second accessor ref for a typed receiver. Refusal releases preparation-owned resources under an uncanceled context. |
| Exact-reference preparation and graph publication | `preparePartDependenciesLocked`, [cache_part_install.go:230](dagql/cache_part_install.go#L230); temporary holds at [line 162](dagql/cache_part_install.go#L162); `applyPartDependenciesLocked` at [line 299](dagql/cache_part_install.go#L299). | Matches the transaction foundation in D6:99–105/123: hold exact rows before typed store decoding; validate cycles through direct and offer ownership before stores; publish direct/reverse edges and propagated Own generations. The donor hold is not an output edge. |
| Imported origin | `IsImportedResult`, [cache_offer_owner.go:298](dagql/cache_offer_owner.go#L298); constructor's `receiver.imported`, [cache_part_source.go:586](dagql/cache_part_source.go#L586). | Matches D6:84: imported origin is independent of persisted-edge retention. The queue must use the graph-visible origin/type facts without forcing typed decode. |

S1 extends the existing owning preparation transaction; none of its public arities need to change. The current concrete transaction is useful, but cannot yet perform D6's ordered multi-part pass.

| Transaction surface | Exact implementation and current checks | S1 work identified against design lines |
| --- | --- | --- |
| Owning preparation | `PreparedReadyPart`, [cache_part_install.go:47](dagql/cache_part_install.go#L47); `PrepareReadyPart`, [line 113](dagql/cache_part_install.go#L113); idempotent `Release` at [line 80](dagql/cache_part_install.go#L80). | D6:242–255 declares `readyPartPreparationBase`, `prepareReadyPartFromBase`, `expectedRepresentation` and `expectedPredecessors`. All four are absent. Prepare currently probes the real row at line 139, stores its observed `version` at 143, and prepares `next` from that current record at 195. The public function must become the nil-base wrapper; the private extension supplies only copied data/provenance. |
| Receiver representation guard | `capturedRowRevision` and its `check`, [cache_output_revision.go:46](dagql/cache_output_revision.go#L46); Commit's pre-E check at [cache_part_install.go:342](dagql/cache_part_install.go#L342), and E→G→P comparison at [line 439](dagql/cache_part_install.go#L439). | Current checks cover typed output-version vectors, payload revision, encoded pointer and representation state. D6:251–266 requires separate original-observation and expected-prefix stamps. Two preparations made from the same real envelope currently conflict after the first increments `payloadRevision` at line 462. There is no predecessor exception or provenance check. Numeric revision adjustment alone would not satisfy S1. |
| Typed prepared store | `PreparedPartStore`, [cache_part_store.go:16](dagql/cache_part_store.go#L16); File/Directory `TryLock` at [core/part_store.go:177](core/part_store.go#L177) and [line 236](core/part_store.go#L236); Container `TryLock` at [line 335](core/part_store.go#L335), `PreparePartStore` at [line 396](core/part_store.go#L396). | File/Directory compare their actual `OutputRev`; Container captures `previous := acquiredOutput.Load()` and computes `baseRevision+1` at lines 397–411. D6:253/266 needs expected prepared-prefix identities/revisions here as well as in DagQL. Existing immutable `containerAcquiredOutput` at line 288 is the data/view foundation. Inline aggregates already have a vector through `capturedOutputVersions`; they have no invented aggregate Container revision. |
| Permit, missing-output, ownership and store checks | `CommitReadyPart`, [cache_part_install.go:319](dagql/cache_part_install.go#L319): consumes once; checks cancellation; donor/current resource facts at 351–404; graph/requirement delta at 405; G at 410; every target Pending at 416; OriginalPermit when applicable at 422; registered writer ticket at 428; nonblocking typed store lock at 431; P at 437. | Matches the existing transaction in D4:307–318. D6:259–264 additionally needs every predecessor's exact owning installation identity, expected earlier edges and the original structural-admission proof. The current target's active task/permit is checked; no prior-address installation identities are compared. |
| Installation identity provenance | `PartTaskToken`, [cache_part_task.go:24](dagql/cache_part_task.go#L24); `partOutputState`, [cache_part_gate.go:72](dagql/cache_part_gate.go#L72); installation publication at [cache_part_install.go:465](dagql/cache_part_install.go#L465). | D6:253 names receiver/key/nonzero generation as the preparation's reserved predecessor identity. That token tuple already exists before preparation, but `PreparedReadyPart` has no copied predecessor list or checks tying it to earlier addresses. The gate also has a separate scalar `installation`, currently chosen only during Commit (`installation := gate.revision`, line 467). S1 must compare the prepared owning identity with the actual prior publication, copying scalars without borrowing task authority; a matching numeric revision alone is insufficient. D6:251 also requires overflow rejection; current increment sites at lines 462/465 and core line 411 have none. |
| Donor validation | Ready donor `source.version.check` at [cache_part_install.go:351](dagql/cache_part_install.go#L351); full `source.facts` equality at [line 363](dagql/cache_part_install.go#L363); `partFactsLocked` at [cache_part_source.go:156](dagql/cache_part_source.go#L156). | **D6:82/263/270 requires donated-address stability.** Current validation compares the donor's whole payload, typed vector, gate revision, offer revision, resource generation, ownership revision and expiry. A sibling installation or unrelated permit changes those facts and refuses an unchanged donated part. Per-address output state exists in the gate, but the required stable donated descriptor/output/applied-role proof does not yet replace these whole-row comparisons. Offer replacement likewise changes retention revisions even when Own is unchanged. |
| Publication and handoff | Non-failing stores start at [cache_part_install.go:442](dagql/cache_part_install.go#L442); complete desired map at 455; encoded or typed publication at 456–462; token publication at 487; owning receipt at 488; deferred cleanup at 323 and `release` at [line 86](dagql/cache_part_install.go#L86). | Matches D6:123/127: Installed survives a cleanup error, temporary dependencies/donor/permit are released before returning, the separate receiver hold transfers to the receipt, and protection belongs to receiver cleanup. The applied role map is not forged at Commit. |
| External Finish | `ReadyPartReceipt`, [cache_part_install.go:65](dagql/cache_part_install.go#L65); `FinishReadyPart`, [line 490](dagql/cache_part_install.go#L490); inline helper at 506; public wrapper at 513. | Matches D6:175–179: external Finish consumes the receipt hold on every return and opens the task barrier. Calling it from the owning Body is rejected at 495. `InstallReadyPart` uses an inline handoff when already inside the task; the sharing worker must use the split functions and externally drain all receipts after its member-release barrier. |

The task and notification contracts are mapped below. E is `Cache.egraphMu`, G is the row gate mutex, P is `payloadMu`, and D is `lazyMu`. No sharing queue or notification collector exists yet.

| Surface | Exact code boundary | Required use or difference |
| --- | --- | --- |
| NoJoin and task ownership | `LazyTaskSpec`, [cache_part_task.go:14](dagql/cache_part_task.go#L14); `RunLazyTask`, [line 116](dagql/cache_part_task.go#L116); `runLazyTask`, [cache.go:4234](dagql/cache.go#L4234); NoJoin refusal at 4254; fresh generation at 4354. | D6:119 is supported. The active/bookkeeping-pending check precedes callback replacement and waiter changes in the same D admission section. Use `PartTaskFromContext` at [cache_part_task.go:48](dagql/cache_part_task.go#L48) and `TryAcquire` at [cache_part_gate.go:147](dagql/cache_part_gate.go#L147). Full-address `obtain` keys are built by `partTaskKey` at [cache_part_demand.go:119](dagql/cache_part_demand.go#L119). |
| Retained bookkeeping and D2 | `lazyTaskContinuation.finish`, [cache_part_task.go:59](dagql/cache_part_task.go#L59): barrier at 67, sync at 75, protection cleanup at 82, settlement at 97; `settlePart`, [cache_part_content.go:305](dagql/cache_part_content.go#L305). | D6:125 describes `Settled` invoking D2; the actual kernel already invokes D2 automatically for token-installed outputs at lines 98–103, then invokes optional `spec.Settled`. Sharing must consume this automatic settlement and avoid a duplicate callback. Native completion now also calls `settlePart` through [completeNativePartTask:140](dagql/cache_part_host.go#L140), as B4-D3 requires. |
| Lazy completion and S2 | Bookkeeping/native settlement at [cache.go:4458](dagql/cache.go#L4458) and 4471; completion/retirement under D at 4476–4506; D unlock at [4506](dagql/cache.go#L4506); waiter wake at [4518](dagql/cache.go#L4518). | D6:142–144 hook belongs **after** `close(attempt.done)` at 4518 and only after successful ownership/settlement. Reenter E, compare `resultsByID[id] == row`, enumerate current classes and acquire member holds in that E interval. A native attempt only has the counted operation here; synthetic tasks additionally hold the row at 4413–4428. Neither replaces the registration check. The existing test hook at 4515 is before the wake and is not the sharing notification point. |
| Actual union and congruence repair | `mergeEqClassesNoRepairLocked`, [cache_egraph.go:274](dagql/cache_egraph.go#L274), moved membership completes at 367 before return 370; `mergeEqClassesLocked` at [373](dagql/cache_egraph.go#L373); repair at [410](dagql/cache_egraph.go#L410), repair-discovered unions at 400. | D6:133/137: record only an actual union after indexes move; recanonicalize pending queue roots there. Thread the E-interval collector through both direct and repair unions. Existing signatures carry no collector. |
| New membership without union | `addResultOutputEqClassLocked`, [cache_egraph.go:1279](dagql/cache_egraph.go#L1279); forward assignment at 1293, reverse assignment at 1300. | D6:138 requires newly inserted `(row, canonical class)` detection. Current assignments are unconditional and report no insertion fact. Add the change record only after both memberships exist. |
| Initial publication, success and rollback | `initCompletedResult`, [cache.go:5717](dagql/cache.go#L5717); indexing E interval begins at [5997](dagql/cache.go#L5997), attachment barrier setup at 6063, success unlock at [6069](dagql/cache.go#L6069). `failPartialPublication` at 6020 rolls back via [5682](dagql/cache.go#L5682), then unlocks at 6027; adopted-row failure unlock is 6022. | D6:139/146: flush immediately before the indexing interval's unlock, after handoff/direct-edge validation/barrier setup, or after rollback filtering on failure. Do not flush from the middle of indexing. The earlier adoption E interval at 5746–5759 only chooses/holds a row; it is not the initial indexing flush. An early queue hold may retain a registered row through a later unlocked failure for one pass. |
| Eager completion | Successful `syncResultSnapshotLeases` at [cache.go:6097](dagql/cache.go#L6097), `registerLazyEvaluation` at 6108, `finishAttachDeps(nil)` at [6109](dagql/cache.go#L6109). | D6:141: after these succeed, reenter E, validate registration and queue current classes while the publication handoff still protects the row. Failure paths at 6071–6089 and 6098–6106 produce no completion notification. Current code returns immediately at 6111. |
| Request identity teaching | `lookupCacheForRequestLocked` teaching at [cache_egraph.go:966](dagql/cache_egraph.go#L966); outer `lookupCacheForRequest` E lock at 997, exits at 1000/1006/[1017](dagql/cache_egraph.go#L1017). | D6:139: propagate the collector through `teachResultIdentityLocked`/`applyPreparedResultIdentityLocked`, then flush at the outer E exit after all index mutations. The simple recipe-hit fast path at 945 has no identity mutation. |
| Explicit call teaching | `TeachCallEquivalentToResult`, [cache_egraph.go:1063](dagql/cache_egraph.go#L1063); E lock at 1110, deferred unlock at 1111, mutation at 1118. | D6:139: flush/discard in this E interval before the deferred unlock, including errors. |
| Content identity teaching | `TeachContentDigest`, [cache_egraph.go:1121](dagql/cache_egraph.go#L1121); E lock at 1185; unlocks at 1189, 1193, 1197, 1202 and [1207](dagql/cache_egraph.go#L1207); final frame store at 1205. | D6:133/139: a fresh collector for every loop iteration's E acquisition. The changed-frame retry at 1197 cannot carry a collector across the unlock. Flush only after frame/posting mutation or the error/rollback decision for that interval. |
| Live import | `ImportValues`, [cache_value_import.go:197](dagql/cache_value_import.go#L197); private preparation through 341; live E interval at [345](dagql/cache_value_import.go#L345), identity publication at 365, final unlock at 367. | D6:140: flush changed roots and imported rows' final classes after all identity applications, before 367. Failed root validation exits at 347 without publishing. Private planning and boot remain unable to enqueue. |
| Queue operation and close | Nonblocking global `beginCacheOperation`, [cache.go:205](dagql/cache.go#L205); `waitForQuiescence` at [357](dagql/cache.go#L357); `Close` at [4540](dagql/cache.go#L4540). | D6:150–168: existing atomic admission can run under E before member holds. The queue/cohort/FIFO/worker, admission flag, cancellation and external receipt drain are absent. Sharing shutdown must precede the wait at 4547; successful persistence is at 4556 and clean marking at 4573. Batch 5 changes the close entry, so integrate there after the binding reset. |

`PartDemandState` is declared in [cache_part_source.go:492](dagql/cache_part_source.go#L492), not in `cache_part_demand.go`. It currently owns `mu`, `exhaustedContent`, `failures` and `revision`. `partContentKey` at [line 499](dagql/cache_part_source.go#L499) includes source ID, full address, SnapshotValue, ordered layer records and the admitted offer revision; B4-D2 is present. `demandPart` creates the state and context binding at [cache_part_demand.go:150](dagql/cache_part_demand.go#L150), `CheckPartSources` receives it at [307](dagql/cache_part_demand.go#L307), `partDemandFromContext` retrieves it at [cache_part_content.go:179](dagql/cache_part_content.go#L179), and Provider receives the same pointer at [223](dagql/cache_part_content.go#L223). Exhaustion/cause accumulation is at [183](dagql/cache_part_content.go#L183). D5:109–113 adds a second, separately keyed renewal-episode map; it is absent at this baseline. Sharing performs no renewal and must leave both demand budgets and their foreground propagation intact.

The persisted-service decoding seam is concrete, but its preparation context is not implemented yet.

| Entry or initialization surface | Exact implementation | D6 assumption and mismatch |
| --- | --- | --- |
| Typed part decode context | `partDecodeContext`, [cache_part_demand.go:28](dagql/cache_part_demand.go#L28); typed preparation at [cache_part_install.go:208](dagql/cache_part_install.go#L208). | Current code uses `CurrentDagqlServer(ctx)` or `row.partGate.server`, then `NewPersistDecodeContext(...).WithSnapshotRoles(...)`. D6:193–208 requires `Cache.SetPartPreparationContext`, a set-once engine callback and preflight before service reconstruction. No registration field/API or nil-callback eligibility decision exists. A remembered foreground server is not the prescribed engine-root preparation context. |
| Service bindings and exact loads | `decodePersistedServiceBindings`, [core/service.go:274](core/service.go#L274); `loadPersistedObjectResultByResultID`, [core/persisted_object.go:100](core/persisted_object.go#L100), via `loadPersistedResultByResultID` at [86](core/persisted_object.go#L86); `PersistDecodeContext.ResultRef` at [cache_persistence_codec.go:199](dagql/cache_persistence_codec.go#L199). | Matches D6:187/212: reconstruct exact Service objects plus hostnames/aliases, using empty-session `loadResultByResultID` at line 210 and the admitted operation's lifetime. These calls enter ordinary shared decode; they are not a safe place to discover known missing preparation capability after entry. |
| Service → Module defaults | `Service.DecodePersistedObject`, [core/service.go:171](core/service.go#L171), ModuleContext load at 180; `Module.DecodePersistedObject`, [core/module.go:1107](core/module.go#L1107), `query.DefaultDeps(ctx)` at [1130](core/module.go#L1130); `Server.DefaultDeps`, [engine/server/session.go:3438](engine/server/session.go#L3438), `executableClientFromContext` at [1523](engine/server/session.go#L1523). | **S3(a), D6:189/204:** the current Module call unconditionally requires a held ClientScope through the engine root. `persistedDecodeDefaultDeps` and `ContextWithPersistedDecodeDefaults` are absent. Add the narrow marked-context default factory at this call; ordinary `Server.DefaultDeps` behavior remains intact. |
| Shared decode and copied-owner publication | `ensurePersistedHitValueLoaded`, [cache_persistence_import.go:645](dagql/cache_persistence_import.go#L645); shared attempt join at [703](dagql/cache_persistence_import.go#L703), error propagation at 717–732, leader publication at 750; copied desired roles at [808](dagql/cache_persistence_import.go#L808), P-only CAS at 827–857; `DecodeResult` at [cache_persistence_self.go:126](dagql/cache_persistence_self.go#L126). | Matches D6:280–288's existing decode transaction. A known context error would reach a foreground joiner, confirming why D6:227 requires preflight before the first exact load. Decode-side lease sync at 866 remains independent of the owning installation's Finish, and cleanup joining at 854 must survive the new writers. |
| Snapshot scopes and explicit empty roles | `NewPersistDecodeContext`, [cache_persistence_codec.go:152](dagql/cache_persistence_codec.go#L152); `WithSnapshotRoles` at 157; `SnapshotScope` at 228 and `SnapshotRoles` at [235](dagql/cache_persistence_codec.go#L235); core role loader at [persisted_object.go:121](core/persisted_object.go#L121). | Matches D6:282–284: a non-nil matching carrier is authoritative even when empty; inline paths select their own scope and exact child rows receive their own copied carrier. No second persisted decode path is needed. |
| Static native schema base | `Server.getCoreSchemaBase`, [engine/server/session.go:367](engine/server/session.go#L367); `schema.NewCoreSchemaBase`, [core/schema/coremod.go:42](core/schema/coremod.go#L42); low-level `dagql.Server.Fork` at [dagql/server.go:238](dagql/server.go#L238). | D6:200/202 requires the static base installed before admission. Currently construction is lazy through client initialization at [session.go:1314](engine/server/session.go#L1314). `CoreSchemaBase.ForkForPersistedDecode` is absent. Existing `CoreSchemaBase.Fork` at [coremod.go:67](core/schema/coremod.go#L67) calls `viewState`, which calls `buildTypeDefs` at 95 and retains cache-backed definitions; it cannot serve as the new schema-only fork. |
| Pure default builder and root binding | `CoreSchemaBase.CoreMod(view)`, [core/schema/coremod.go:60](core/schema/coremod.go#L60); `NewSchemaBuilder`, [core/moddeps.go:51](core/moddeps.go#L51), `Append` at 89; `NewUserMod` at [core/module.go:2254](core/module.go#L2254); `persistedDecodeQuery` at [core/persisted_object.go:30](core/persisted_object.go#L30). | Existing constructors provide D6:204–206's data-only factory ingredients. Ordinary equivalent builder construction is [session.go:1324](engine/server/session.go#L1324). The new helper must validate the registered root against `dec.Server()` and create fresh builders for that server's view, including nested Module decodes. |
| Context/server helpers and fallback loaders | `ContextWithQuery`, [core/query.go:230](core/query.go#L230); `ContextWithCache`, [dagql/server.go:2186](dagql/server.go#L2186); cache-private `srvToContext` at [2198](dagql/server.go#L2198); `InstallCoreSchemaLoaders` at [core/schema_build.go:22](core/schema_build.go#L22). | These provide D6:208's binding operations. `core.CurrentDagqlServer` at [query.go:242](core/query.go#L242) otherwise falls back to `Query.Server.Server(ctx)` at 254. Loader fallbacks call module schema resolution or inspect client metadata at schema_build lines 28–49. Preflight must ensure all needed native classes resolve directly. |
| Registration point after restored ownership | `NewServer`'s successful `initLocalCacheState` return at [engine/server/server.go:267](engine/server/server.go#L267); `initLocalCacheStateOnce` creates the cache at [630](engine/server/server.go#L630), checks reset at 634 and releases old transfer leases at 639; normal return at 643. | D6:200/305's ordering maps to the successful outer initialization boundary after line 269, after owner restoration and startup transfer cleanup, before request/import admission. There is no preparation callback registration there yet. Batch 5 places its adapter in this startup region too; the preparation context must be ready before that adapter can admit imports/control work. Cache-only core fixtures must supply their own engine-root equivalent; DagQL-only fixtures may leave the callback nil and assert the service-dependent skip. |
| Worker operation lease context | `ContextWithOperationLeaseProvider`, [dagql/operation_lease.go:22](dagql/operation_lease.go#L22); engine lazy-lease provider construction at [session.go:1305](engine/server/session.go#L1305), also 2442. | D6:164/208 requires an engine/cache lifetime base without requesting-client metadata/scope. Existing provider closures supply `WithLazyLease` using the engine lease manager; no clean sharing worker base exists yet. Do not retain a request context with `WithoutCancel` as the worker authority. |

For D6:218–227's decoder-family inventory, the exact entry points read in this tree are listed here. This is a source inventory for the future explicit background-admission registry, not runtime proof that a shared sessionless attempt is already safe.

| Native family | Decode entry points and relevant form distinction |
| --- | --- |
| Service and Module ancestry | [Service.DecodePersistedObject:171](core/service.go#L171), [Module.DecodePersistedObject:1107](core/module.go#L1107), [ModuleSource.DecodePersistedObject:921](core/modulesource.go#L921). ModuleSource reconstructs persisted SDK configuration/capabilities at 1016; it does not resolve an SDK there. |
| File and Directory | [File.DecodePersistedObject:395](core/file.go#L395) → [decodePersistedFileWithSnapshotRole:334](core/file.go#L334); [Directory.DecodePersistedObject:417](core/directory.go#L417) → [decodePersistedDirectoryWithSnapshotRole:356](core/directory.go#L356). Pending transfer forms keep raw producers; snapshot forms store their recipe bytes; native lazy forms invoke their lazy decoders at File 384 / Directory 406. A blanket exclusion of every recipe payload would miss actual native decode references. |
| Container | [Container.DecodePersistedObject:1505](core/container.go#L1505). Metadata references load before the raw-view branch at 1611; raw producer state is preserved at 1624. Ordinary pending native recipes enter `decodePersistedContainerRecipe` at 1644; settled recipes retain bytes at 1638. Typed sharing preparation enters this decoder through [Container.PreparePartStore:396](core/part_store.go#L396) and reconstructs donated-part service bindings through [assignAcquiredRef:423](core/part_store.go#L423). |
| Type definitions | [TypeDef:866](core/typedef.go#L866), [ObjectTypeDef:1234](core/typedef.go#L1234), [InterfaceTypeDef:1639](core/typedef.go#L1639), [ScalarTypeDef:1804](core/typedef.go#L1804), [ListTypeDef:1856](core/typedef.go#L1856), [InputTypeDef:1928](core/typedef.go#L1928), [EnumTypeDef:2033](core/typedef.go#L2033), [EnumMemberTypeDef:2214](core/typedef.go#L2214), [FieldTypeDef:1522](core/typedef.go#L1522), [Function:101](core/typedef.go#L101), [FunctionArg:670](core/typedef.go#L670), [SourceMap:2632](core/typedef.go#L2632), all `DecodePersistedObject`. Their `decodePersisted*` helpers at 2831–3473 reconstruct data and load the declared type/function/source-map rows. |
| Workspace and Changeset | [Workspace.DecodePersistedObject:811](core/workspace.go#L811), [decodePersistedWorkspaceSource:709](core/workspace.go#L709), [Changeset.DecodePersistedObject:459](core/changeset.go#L459), [decodePersistedChangesetPayload:434](core/changeset.go#L434). The source helper handles inline overlay ancestry and exact Directory/GitRef/Changeset rows. |
| Git | [GitRepository.DecodePersistedObject:633](core/git.go#L633), [decodePersistedRemoteGitRepository:562](core/git.go#L562), [GitRef.DecodePersistedObject:707](core/git.go#L707), [GitCommit.DecodePersistedObject:767](core/git.go#L767). The invoked backend `Get` implementations only wrap repo/ref data at [git_local.go:34](core/git_local.go#L34) and [git_remote.go:129](core/git_remote.go#L129). |
| Local mutable mirror ancestor | [RemoteGitMirror.DecodePersistedObject:138](core/git_remote_mirror.go#L138). Foreign uninitialized form returns at 149; ordinary persisted form reopens its already-owned mutable snapshot at 162 with `NoUpdateLastUsed`. D6 permits this decode ancestor while excluding the mutable backing as a donor; a storage failure keeps its cause. |
| Resource/data ancestors | [CacheVolume.DecodePersistedObject:401](core/cache.go#L401), [Volume.DecodePersistedObject:259](core/volume.go#L259), [Secret.DecodePersistedObject:64](core/secret.go#L64), [Socket.DecodePersistedObject:79](core/socket.go#L79), [EnvFile.DecodePersistedObject:61](core/envfile.go#L61). Volume reconstructs exact SSHFS Secret/Service references without mounting. |
| Existing registry and visitor | `persistedObjectFamilies`, [core/persisted_families.go:12](core/persisted_families.go#L12); `PersistedObjectFamilyByName`, [cache_persistence_codec.go:585](dagql/cache_persistence_codec.go#L585); `VisitEncodedReferences` at [620](dagql/cache_persistence_codec.go#L620). This registry describes persistence, not background admission. The general visitor also visits the recorded call at 650; it is not by itself the closure of references a given native decoder actually loads. The separate audited registry and form-aware preflight required by D6:227 and the round-2 guidance are absent. |

D6:229's `engine.WithSnapshotSharePreparation`, `CheckSnapshotSharePreparation` and `ErrSnapshotShareEvaluation` are absent. Concrete guard insertion boundaries are [Cache.Evaluate:3956](dagql/cache.go#L3956), [Cache.EvaluateParts:4021](dagql/cache.go#L4021), [PartHost.Evaluate:29](dagql/cache_part_host.go#L29), [PartHost.RunNative:82](dagql/cache_part_host.go#L82), [demandPart:144](dagql/cache_part_demand.go#L144), [runPartProducerDecision:368](dagql/cache_part_demand.go#L368) before invocation at 446, [LazyState.Evaluate:58](core/lazy_state.go#L58)/[EvaluateGroup:106](core/lazy_state.go#L106), [Service.Start:510](core/service.go#L510), [Services.Start:594](core/services.go#L594)/[StartBindings:749](core/services.go#L749), [SchemaBuilder.Schema:163](core/moddeps.go#L163)/[TypeDefs:196](core/moddeps.go#L196), [CoreSchemaBase.viewState:80](core/schema/coremod.go#L80), [executableClientFromContext:1523](engine/server/session.go#L1523), and the content-source/provider boundary below. **Naming mismatch:** D6:229 calls out batch 4 `EnsureOutput`, but no such production symbol exists; the host/acquired-demand functions above implement that responsibility, including an inline path that directly enters `evaluateAcquiredScope`. This is an interface mapping to preserve when placing guards, not authorization to introduce another demand API. Synthetic obtain Bodies, exact native decode, local pin/open and independent row sync must stay usable under the marker. The guard is a diagnostic boundary; the preflight/audit must prevent known context-dependent errors from entering the shared attempt. Required S3(a) costs remain preflight traversal, guard checks on unmarked calls, earlier static core-base construction and maintaining the admitted-family audit.

The batch 5 coordination inventory is derived from its design, including surfaces absent from this baseline. These are to be reconciled through the binding batch 5 head and the user; no other implementer was contacted.

| Batch 5 surface and design lines | Baseline anchor and contract batch 6 must preserve |
| --- | --- |
| `Cache.OfferParts`, `OfferOutcome`, `OfferDisposition`; D5:21–65 | Absent in this tree. Uses `probePart`, the row `PartWriterGate` and the full-address mapping. Acceptance/replacement is one E→G transaction, Open/Preparing can accept, Running/Consumed overlap refuses, Installed/Complete refuses. It neither starts a task nor changes lookup resources. Return full-length ordered dispositions, including Accepted entries before a later error. Queue changes must not add a speculative acquisition or wait to offer acceptance. |
| Exact offer ownership and address-only refresh; D5:44/61/65/67/73–81 | `newOfferOwnerLocked`, `retainOfferOwnerLocked`, `releaseOfferOwnerLocked` at [cache_offer_owner.go:54](dagql/cache_offer_owner.go#L54), 81, 87; attach/replace/retire at [175](dagql/cache_offer_owner.go#L175), 196, 216. Preserve canonical owner IDs, one preparation hold on same-owner refresh, independent old acquisition holds, and separate `transferRevision`/`dependencyOwnershipRevision` increments. Offer-only refs remain outside Own at every depth. Sharing takes no offer acquisition hold. |
| D1, current-source proof and producer start; D5:46/50/63/67 | `offerAllowedLocked`, [cache_part_source.go:235](dagql/cache_part_source.go#L235); `SourceCheck`, [cache_part_gate.go:106](dagql/cache_part_gate.go#L106); `CheckPartSources` and `sourceCheckCurrentLocked`, [cache_part_demand.go:307](dagql/cache_part_demand.go#L307)/345. Batch 5's offer revisions invalidate final source scans while preserving frozen writer tickets. Sharing must not alter foreground route ranking, exhaustion, ordinary lookup or native/private producer decisions. |
| D2 and replacement settlement; D5:75–81 | `settlePart` → `retireFinalPartOffersLocked` → `retirePartOfferLocked`, [cache_part_content.go:305](dagql/cache_part_content.go#L305), 334, [cache_offer_owner.go:216](dagql/cache_offer_owner.go#L216). Under E→G retire the current full-address slot, including a replacement, and mark Complete. Preserve siblings and ordinary edges. Both native and acquisition continuations use it already. Sharing invokes no retirement primitive directly. |
| Stable `PartContentSource`, constructor, getter, `Available`, `Provider`; D5:91–99/175–194 | Current [cache_part_content.go:19](dagql/cache_part_content.go#L19) is an interface with `Available(offer, int64)` and `SetPartContentSource` at 26, plus a fixed fallback at 34. D5 replaces that surface with a concrete source, `NewPartContentSource(http.RoundTripper)`, stable `Cache.PartContentSource()` and `time.Time` availability. Place D6's refusing provider around the converged batch 5 boundary; preserve nil-integration fixed URLs, local-only ImportChain opportunity, known-metadata Info, transport settings and response ownership. Marked `Info` and `ReaderAt` must refuse before delegate/renewal/network work. |
| Demand exhaustion and renewal episodes; D5:105–113 | Current `PartDemandState` and propagation are cited above. Preserve the batch-4 per-source/revision exhaustion map plus batch-5 separate `(target full address, ordered blob-content key)` episode state/result. Source/revision/key/expiry rotation must not create another episode for the same bytes. Exact chain fingerprint and content-budget key have distinct purposes. Sharing never claims an episode. |
| Provider HTTP behavior and `snapshots.ChainContentError`; D5:105/115–123/226–230 | Existing provider selection/import at [cache_part_content.go:210](dagql/cache_part_content.go#L210). Preserve `renewalRetryStatuses` (401/403/404/410), one renewed-address episode, private reply address maps, Range/sequential response handling, Close cancellation and 30-second network idle accounting. Keep remote content errors distinct from local snapshot/writer/lease errors. A local sharing open/decode error is not renewable content failure. |
| `RenewalRequestID`, `RenewalRequest`, `RenewalReply`, `RenewalReplyDisposition`, `RemoteCacheBridge.TakeRenewalRequest`/`ReplyRenewal`; D5:131–161 | Absent at baseline. Preserve one-shot epoch/sequence correlation, exact chain fingerprint, Done, two-second total deadline, and the bound of 64 queued-plus-delivered requests. Queue sharing state must be separate from this mailbox. M is never nested with E/G/D/P or the demand mutex; no callback under those locks. |
| `AttachRemoteCacheBridge`/`DetachRemoteCacheBridge`; D5:181–202 | Absent at baseline. Repeated attach returns the same bridge; stale detach cannot close a successor; shutdown closes admission and detaches before drain. Sharing's worker cancellation and queue admission must compose with this lifecycle without touching pending ordinary offer slots. |
| `NewServerOpts.RemoteCacheIntegration`, `RemoteCacheIntegrationConfig.Run`, `newRemoteCacheAdapter`, adapter `OfferParts`/`TakeRenewalRequest`/`ReplyRenewal`/`Stop`; D5:167–204 | Planned insertion beside [server startup:267](engine/server/server.go#L267). Preserve nil/off allocation behavior, one consumer per attachment, callback-exit detach, stop idempotence and bounded join. Coordinate startup ordering with `SetPartPreparationContext` so external control cannot precede preparation registration. No new endpoint, listener or integration transport is needed for sharing. |
| Cache/server shutdown and `CloseWithShutdownError`; D5:232–240 | Current [Cache.Close:4540](dagql/cache.go#L4540) and [Server.GracefulStop:829](engine/server/server.go#L829). D5 adds `CloseWithShutdownError(ctx, cause)` sharing `closeOnce`, preserves adapter-stop errors through both final returns and prevents a clean checkpoint on failed adapter join. Sharing must cancel/wake and drain before cache quiescence/persistence without losing that seeded error or extending the engine deadline. A failed Close cannot later become a clean checkpoint. |
| Checkpoint, boot and onward capture; D5:206–216 | Pending offer snapshot/owner records at [cache_offer_owner.go:285](dagql/cache_offer_owner.go#L285), capture revisions at [cache_value_capture.go:95](dagql/cache_value_capture.go#L95), desired-map projection at [cache_persistence_worker.go:96](dagql/cache_persistence_worker.go#L96), `restoreOfferOwnersLocked` at [cache_offer_owner.go:325](dagql/cache_offer_owner.go#L325), local owner attachment at [cache_persistence_import.go:581](dagql/cache_persistence_import.go#L581), `settleRestoredOffers` at [cache_offer_restore.go:52](dagql/cache_offer_restore.go#L52). Preserve schema 20/envelope 4/bundle 1, Imported origin, exact producer/output edges, pending sibling offers, owner-reference relocation and owner-before-transfer-pin cleanup. No queue/receipt/episode/provider state becomes durable; installed-but-unsettled live export stays not-ready. |
| Verification/fixture interfaces; D5:256–271 | Preserve offer-before-start/ownership/replacement/resource tests, renewal exhaustion/mailbox/reader/server-lifetime tests and native cancellation/bookkeeping/sibling concurrency. The existing `_remoteCacheFixture` and `_DAGGER_TEST_REMOTE_CACHE_FIXTURE_ROOT` remain the only native fixture entry. Batch 7 owns its larger controls and full pipeline extension; batch 6 still owns its focused real-store, restart, encoded Service/Module joiner and preparation-guard proofs in D6 §9. |

## Producer foundation dependencies for the proposal

This section incorporates the coordinator's steering while keeping the baseline fixed. D6 does not name `RecordCompletedProducer` or `completedRecipe` directly. Its requirements to retain producers, copy complete views, avoid producer execution, and preserve producer edges consume the concrete symbols below. These are the design and intended implementation touch points; the replacement foundation must supply their surviving guarantees before batch 6 implementation starts. No replacement API or new mechanism is assumed here.

| Batch 6 assumption or planned touch | Design lines | Exact baseline code and consequence for the proposal |
| --- | --- | --- |
| Foreground fallback after sharing skips | D6:16, 23, 34, 172, 323–325 | `demandPart` routes at [cache_part_demand.go:193](dagql/cache_part_demand.go#L193), selects local/offered sources at 203, and invokes `runPartProducerDecision` at 264. `PartProducerRoute`/`PersistedPartRouter` are [cache_part_routes.go:5](dagql/cache_part_routes.go#L5)/20. D6's explicit local → offered chain → saved producer wording describes the outgoing foundation. Batch 6 requires later ordinary demand to retain its correct input identity and errors; the new unified lazy path, rather than this obsolete route, will define how that happens. Sharing itself still has no producer fallback. |
| Probe depends on producer routing even without executing it | D6:49–69, 74, 80–84, 150, 172, 354 | `DescribeParts` calls `RouteParts` for every mapped output at [core/part_probe.go:15](core/part_probe.go#L15), then populates `HasProducer` at 19. `foreignFamilyCodec.RouteParts`, [core/part_routes.go:52](core/part_routes.go#L52), interprets File/Directory `LazyKind` at 63/80 and Container `LazyJSON` plus recorded field at 97–109; metadata-dependent write sets appear at 122–145. The proposed sharing probe therefore cannot simply keep the old describer unchanged after the separate route disappears. It still needs pure full-address mapping and completion facts, and must not exclude a pending receiver because it is re-creatable or evaluate metadata to discover a mount. |
| Producer and sharing writer exclusion | D6:80, 119, 314, 323, 356–358 | `TryAcquire`, [cache_part_gate.go:147](dagql/cache_part_gate.go#L147), rejects overlapping Running/Consumed and Preparing producer groups at 171–175; group state is [line 77](dagql/cache_part_gate.go#L77). Native `PartHost.RunNative` installs its group at [cache_part_host.go:109](dagql/cache_part_host.go#L109) and output token at 124. Sharing's NoJoin obtain task consumes this common gate, never a producer decision task. The new lazy foundation must preserve that exclusion for both already-evaluated values and pending native work, including disjoint Container parts. |
| Private producer preparation, run and release | D6:119, 172, 206, 229–231, 361–362, 367, 371 | `PersistedPartProducerFactory`/`PartProducerInvocation` are [cache_part_demand.go:19](dagql/cache_part_demand.go#L19)/22. `runPartProducerDecision` prepares at [403](dagql/cache_part_demand.go#L403), enters Original at 438, counts entry at 445, runs at 446, captures at 449 and publishes at 459. Core's `PreparePartProducer`, [part_producer.go:23](core/part_producer.go#L23), builds private Directory/File shells at 42/57 and a Container shell at 78–84; the latter runs whole/group work at 95–107. `privatePartProducer.Run` is [line 18](core/part_producer.go#L18), Release at 22. The intended preparation guard currently names these direct execution boundaries. When the private route goes away, its guard/counter targets must move to the actual unified lazy entry; guards must still permit synthetic sharing installation Bodies. |
| Shared transaction currently also publishes private producer outputs | D6:109–127, 237, 247, 255, 266, 334, 337–338 | `publishProducedParts`, [cache_part_producer.go:31](dagql/cache_part_producer.go#L31), invokes the same Commit at 46 and inline Finish at 48. `prepareProducedParts` sets `original`, `beforeSyncCleanup` and a foreground source at 62, constructs a complete view at 113–129 and uses typed batch/single stores at 193/199. Commit validates `p.original` at [cache_part_install.go:422](dagql/cache_part_install.go#L422), registers private cleanup at 452, and transfers it into the installed token at 483. S1 extends this shared `PreparedReadyPart` representation; it must be designed against whatever producer publication remains on the new head, rather than accidentally retaining obsolete private-run fields or changing ordinary lazy cleanup. |
| Concrete completed-producer storage and completion transitions | D6:16, 23, 34, 221, 251, 294 | File fields are [file.go:48](core/file.go#L48), Directory fields [directory.go:57](core/directory.go#L57), and Container fields [container.go:140](core/container.go#L140). `finishLazyLocked` currently saves and clears the operational pointer at [filesystem_output.go:86](core/filesystem_output.go#L86)/140; Container `consumeLazyOp` saves it at [container_parts.go:346](core/container_parts.go#L346). Sharing's representation copying and readiness code must consume the replacement evaluated-lazy state instead of relying on `Lazy == nil` or retaining these fields. A value having a lazy producer must not by itself make an already-owned complete snapshot unready. |
| Typed File/Directory sharing stores directly write outgoing fields | D6:121, 187, 249–257, 266, 280–288, 338 | File `Publish` writes completed kind/JSON, clears completedRecipe and clears Lazy at [part_store.go:205](core/part_store.go#L205)–209; Directory does the same at [264](core/part_store.go#L264)–268. Their preparation decodes snapshot-form values at 217/276 and explicitly clears the temporary Lazy at 222/281. These are direct batch 6 consumption points, not merely incidental serialization fields. Replacement stores must install output while preserving the receiver's one lazy identity, finality and future persistence contract. S1 cannot copy the old state-clearing behavior into its ordered preparation seam. |
| Raw Container view and ordered preparation preserve producer bytes | D6:187, 221, 227, 249–266, 294, 338 | `PreparePartRecord`, [part_store.go:14](core/part_store.go#L14), modifies File/Directory output form/state at 28–33/47–52 while leaving LazyKind/JSON; Container synthesizes `ProducerState` at 63–65 and retains LazyJSON while replacing only the selected part. `containerAcquiredOutput` stores the full payload at [288](core/part_store.go#L288); Container `PreparePartStore` insists on that raw representation at 407–409. S1's immutable prefix assumes this complete view and cannot change the producer. If the unified lazy foundation changes the raw adapter or payload shape, ordered preparation and the full-view encoder must be remapped to the new representation before implementation. |
| Output stamps and nonblocking native guards fall back to completedRecipe | D6:80–82, 253, 263, 266, 317 | Container `PersistedOutputRevision` falls back from Lazy at [part_store.go:303](core/part_store.go#L303)–308; `tryPartPublicationGuard` repeats that choice at [573](core/part_store.go#L573)–594. `lockForPersistence` also falls back at [container_persistence.go:101](core/container_persistence.go#L101)–106. File/Directory guards and revisions live at [filesystem_output.go:52](core/filesystem_output.go#L52)/106. The replacement foundation must expose stable complete-output stamps and keep body/group exclusion without requiring removal of the lazy producer. Sharing's donated-address proof must remain independent of a disjoint sibling publication. |
| Exact producer/call dependency edges contribute to Own | D6:16, 23, 99–105, 251, 294, 376 | `AttachDependencyResultsKinds` chooses Lazy or completedRecipe at [file.go:111](core/file.go#L111), [directory.go:120](core/directory.go#L120), and [container.go:1168](core/container.go#L1168). These dependencies feed ordinary publication and the Own aggregation at [cache.go:611](dagql/cache.go#L611). The recorded-input helper is [completed_producer.go:207](core/completed_producer.go#L207). The new lazy representation must still attach exact inputs and retain their direct edges; sharing adds exact output/service/handle edges without dropping those original inputs or treating offer edges as requirements. This affects the original-set admission proof and its repeat at Commit. |
| Decode preflight distinguishes retained data from actual lazy reconstruction | D6:189, 214, 221, 227, 229–231, 361–362 | File snapshot decode stores raw completed kind/JSON at [file.go:374](core/file.go#L374), while native lazy decode reconstructs at 384; Directory equivalents are [directory.go:396](core/directory.go#L396)/406. Container raw transfer decode returns an immutable view at [container.go:1624](core/container.go#L1624), settled native decode stores raw bytes at 1639, and pending native decode reconstructs at 1644. Visitors at [persisted_visitors.go:369](core/persisted_visitors.go#L369), 376 and 386 also traverse recipe references. S3's future form-aware preflight must be re-audited if completed native values now reconstruct lazy recipes: previously raw-only references may become actual decoder loads. Their complete Service/Module ancestry must be checked before entering a shared attempt, while reconstruction must still perform no evaluation. A blanket exclusion of recipe references cannot implement this. |
| Native and eager completion triggers change coverage | D6:28–29, 141–144, 350–351, 354, 373 | Eager completion currently follows [cache.go:6097](dagql/cache.go#L6097) through 6109; lazy completion settles at [4458](dagql/cache.go#L4458)/4471, retires under D, then wakes waiters at 4518. Native output admission/publication is [cache_part_host.go:82](dagql/cache_part_host.go#L82), with D2 at 140. Moving cheap eager fields to lazy paths shifts their positive donor notification to successful lazy bookkeeping; a cheap value's initial publication alone will no longer establish an applied output. S2 still requires the waiter wake before the sharing hook takes E. Tests described as eager donors must be reconciled with the new evaluated-lazy model and any truly eager values that remain. |
| Eager recording constructors used to create candidate values | D6:16, 23, 74, 141, 350, 354, 371, 373 | `RecordCompletedProducer` is [completed_producer.go:74](core/completed_producer.go#L74); empty Directory calls it at [schema/directory.go:448](core/schema/directory.go#L448). Two mount eager branches invoke `RecordCompletedContainerMountProducer` at [schema/container.go:2493](core/schema/container.go#L2493)/2729; the recorder is [completed_producer.go:15](core/completed_producer.go#L15). `BuiltInContainer` runs eagerly at [builtincontainer.go:15](core/builtincontainer.go#L15), then records at 21/27. Another production recorder is the schema JSON File at [schema/query.go:243](core/schema/query.go#L243). Batch 6 plans no new calls to these helpers; donor construction and verification must use the foundation's replacement ordinary operations. The schema JSON case is included because the completed-producer API has an additional caller beyond the three cheap-field examples in the steering. |
| HTTP and Git resolved identity, and all remaining production recorder callers | D6:23, 74, 172, 223, 371, 373, 385 | HTTP records the fetched body digest at [schema/http.go:170](core/schema/http.go#L170) after Resolve at 163. Git records bundle import at [schema/git.go:1283](core/schema/git.go#L1283), cleaned local output at 1565, ref tree at 1763 and commit tree at 1865; ref-tree content teaching follows at 1776. The specified model is visible in `container.from`: resolve to digest at [schema/container.go:1384](core/schema/container.go#L1384), reselect using that address at 1443. The proposal's identity-preserving internal calls must remain the source of ordinary equivalence and exact dependencies used by sharing. Immutable HTTP Files/Git trees remain potential donors; mutable HTTP state/mirrors do not. Batch 6 must not add its own re-resolution or identity classifier. |
| Transfer, checkpoint and onward export retain the operation | D6:23, 41–42, 251, 294–305, 338, 373–376 | File/Directory snapshot encoders read the completed fields at [file.go:290](core/file.go#L290)/[directory.go:306](core/directory.go#L306); Container chooses Lazy/completed/raw bytes at [container_persistence.go:56](core/container_persistence.go#L56)–85 and exposes the tuple at 150. Foreign normalization derives `ProducerState` at [value_transfer.go:42](core/value_transfer.go#L42), 53 and 68; its state/validation functions are at 132/141. Sharing assumes the established format cut and complete role projection, but its producer-state and raw-byte preservation clauses describe the old foundation. Any format/state change belongs to that foundation decision, and batch 6 must consume the new durable contract while preserving Installed ownership, pending siblings and exact input edges across restart. Boot still must not execute the operation. |
| Focused and native verification depend on the actual execution boundary | D6:346, 350–354, 361–364, 367, 371–377, 385 | Existing entry accounting is [cache_part_demand.go:445](dagql/cache_part_demand.go#L445); private ref-release observation is [part_producer.go:88](core/part_producer.go#L88)/131. `TestPartAcquisitionRootRoutes`, [part_acquisition_test.go:97](core/part_acquisition_test.go#L97), enumerates producer/fallback cases; `TestPartWholeProducerMixedRestart`, [part_whole_restart_test.go:23](core/part_whole_restart_test.go#L23), checks preserved recipe bytes at 100 and private entry/release at 110–117. Batch 6's zero-execution assertions must count the replacement lazy body, not a deleted private-run event. `ServicesAndDecode`'s raw-producer setup, eager donor controls and retry-with-zero-producer cases need new fixtures that retain the same behavioral assertions. D6:385's batch-7 failure-forced saved-producer scenario likewise becomes ordinary lazy evaluation of the exact imported input after donor removal/restart. These are test obligations to coordinate, not tests run for readiness. |

Verification performed for this readiness commit: `go build ./...` completed successfully with exit status 0 and no output. The user explicitly permitted this compile check during preparation. No tests, race runs, engine-dev runs, snapshot peers or infrastructure operations were run; compilation is not proof of the future sharing behavior.

Read-command failures were retained as failures: one `rg` invocation returned status 2 because `core/part_acquisition.go` and `core/container_acquisition.go` do not exist; another returned status 2 for nonexistent `core/part_routing.go`. Subsequent file inventory located the actual `core/part_store.go` and `core/part_probe.go` sources used above. A `git rev-parse HEAD:hack/designs/remote-cache/focused/06-snapshot-sharing.md` presence check returned status 128 because that design is absent from HEAD; the successful pinned `git show` reads, not that failed check, supply all design evidence. No runtime failure has been hidden by changing the verification selection.

During the producer inventory, an `rg` command also reported `core/transfer*: No such file or directory`; its pipeline ended with status 0 because the final `head` command succeeded. That status does not make the search successful. The relevant `core/value_transfer.go` and `core/persisted_visitors.go` sections were subsequently read directly with successful commands and provide the cited evidence.

All implementation work, including any resolution needed beyond the accepted S1/S2/S3(a) mechanisms, remains behind the binding start message. If a different mechanism becomes necessary, report it through the user with at least two options before implementing it. Preserve the signed evidence commit across the required reset, and use `git commit -s` with `Signed-off-by: Erik Sipsma <erik@sipsma.dev>` on every subsequent commit. No pushes, PRs, tags, agent transcripts, prohibited archives or inter-agent contacts were used.
