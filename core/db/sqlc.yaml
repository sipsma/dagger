version: "2"
sql:
  - engine: sqlite
    schema: ./schema.sql
    queries: ./queries.sql
    gen:
      go:
        package: "db"
        out: "."
        output_db_file_name: "db.gen.go"
        emit_prepared_queries: true
